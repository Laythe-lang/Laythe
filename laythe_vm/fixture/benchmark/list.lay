let start = clock();

for i in range(10000) {
  let collection = [];
  for j in range(1000) {
    collection.push(nil);
    collection[j] = j;
  }

  let sum = 0;
  for j in range(1000) {
    sum = sum + collection[1000 - j - 1];
    collection.pop();
  }
}

print(clock() - start);
