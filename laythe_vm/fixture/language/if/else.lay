// Evaluate the 'else' expression if the condition is false.
if true { assert(true); } else { assert(false); }
if false { assert(false); } else { assert(true); }

// Allow block body.
if false { nil; } else { assert(true); }
