// Evaluate the 'else' expression if the condition is false.
if (true) assert(true); else print assert(false); // expect: good
if (false) assert(false); else assert(true); // expect: good

// Allow block body.
if (false) nil; else { assert(true); } // expect: block
