var small = [];
for (var j = 0; j < 1000; j = j + 1) {
  small.push(j);
}

var large = [];
for (var j = 0; j < 100000; j = j + 1) {
  large.push(j);
}

var start = clock();

for (var i in large) {
  var collection = [];
  for (var j in small) {
    collection.push(nil);
    collection[j] = j;
  }

  var sum = 0;
  for (var j in small) {
    sum = sum + collection[1000 - j - 1];
    collection.pop();
  }
}

print clock() - start;
